
import React from 'react';

import { Link, View } from '../../Core';
import { IType } from '../../Types';

import * as Descriptors from './Descriptors';

export const IUpdatePersistentSubscription: IType = {
  key: 'IUpdatePersistentSubscription',
  descriptor: Descriptors.IUpdatePersistentSubscription,
};

