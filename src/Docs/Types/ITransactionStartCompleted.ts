
import React from 'react';

import { Link, View } from '../../Core';
import { IType } from '../../Types';

import * as Descriptors from './Descriptors';

export const ITransactionStartCompleted: IType = {
  key: 'ITransactionStartCompleted',
  descriptor: Descriptors.ITransactionStartCompleted,
};

