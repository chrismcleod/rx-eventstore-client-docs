export const IResolvedIndexedEvent = `
interface IResolvedIndexedEvent {

  /** ResolvedIndexedEvent event */
  event?: (IEventRecord | null);

  /** ResolvedIndexedEvent link */
  link?: (IEventRecord | null);
}

`;
